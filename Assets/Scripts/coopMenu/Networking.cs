/*
    this script is responsible for handling network traffic for coop games, 
    such as LAN connections or also global games via server connection 

    @author: wek56ur@gmail.com
    @date: 2026-01-08

*/

using UnityEngine;


public class Networking : MonoBehaviour //not sure if gotta be MB
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    //should initiate connection and setup network objects 
    void Start()
    {
        
    }

    // Update is called once per frame
    //runs TCP/IP stacks and updates data
    void Update()
    {
        
    }
}
